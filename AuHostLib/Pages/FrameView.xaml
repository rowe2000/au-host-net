<?xml version="1.0" encoding="utf-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:AuHost.Pages;assembly=AuHostLib"
             x:Class="AuHost.Pages.FrameView" x:DataType="Frame">
    <ContentView.Content>
        <StackLayout Orientation="Vertical">
            <StackLayout Orientation="Horizontal">
                <Button Text="Add rack" Clicked="OnAddRackClicked"></Button>
            </StackLayout>
            <StackLayout x:Name="RackStack" Orientation="Vertical" />
        </StackLayout>
    </ContentView.Content>
</ContentView>