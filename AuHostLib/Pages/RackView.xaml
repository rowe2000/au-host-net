<?xml version="1.0" encoding="utf-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:AuHost.Pages;assembly=AuHostLib"
             x:Class="AuHost.Pages.RackView" x:DataType="pages:RackView">
    <ContentView.Content>
        <StackLayout Orientation="Vertical">
            <Picker x:Name="MidiInPicker" ItemsSource="{Binding MidiInputs}" ItemDisplayBinding="{Binding PortName}" />
            <StackLayout x:Name="ZoneStack" Orientation="Vertical" />
        </StackLayout>
    </ContentView.Content>
</ContentView>
