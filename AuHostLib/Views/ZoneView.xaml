<?xml version="1.0" encoding="utf-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:plugins="clr-namespace:AuHost.Plugins;assembly=AuHostLib"
             xmlns:viewModels="clr-namespace:AuHost.ViewModels;assembly=AuHostLib"
             x:Class="AuHost.Views.ZoneView">
    <ContentView.BindingContext>
        <viewModels:MainPageViewModel>
        </viewModels:MainPageViewModel>
    </ContentView.BindingContext>
    <ContentView.Content>
        <StackLayout Orientation="Vertical">
            <StackLayout Orientation="Horizontal">
                <Button Text="Split"/>
                <StackLayout x:Name="StripStack" Orientation="Vertical" />
            </StackLayout>
            <Button Text="Add strip" 
                    Command="{Binding AddNewStripCmd}" 
                    CommandParameter="{Binding SelectedZone}">
            </Button>
        </StackLayout>
    </ContentView.Content>
</ContentView>